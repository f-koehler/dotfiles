" specify default size of the floating window
let g:floaterm_width=0.8
let g:floaterm_height=0.8

" close terminal when job finished
let g:floaterm_autoclose=1

" do not show title of terminal
let g:floaterm_wintitle=0

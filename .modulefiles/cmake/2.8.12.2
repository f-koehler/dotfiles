set version 2.8.12.2
proc ModulesHelp { } {
    global pkgroot version
    puts stderr "CMake $version"
    puts stderr "CMake, the cross-platform, open-source build system"
    puts stderr ""
}

module-whatis "Kitware CMake 2.8.12.2"

set pkgroot /opt/toolchain/cmake/2.8.12.2

prepend-path    PATH   $pkgroot/bin

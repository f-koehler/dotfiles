- name: configure vscodium 
  file:
    src: "~/.config/Code - OSS"
    dest: "~/.config/VSCodium"
    state: link
    force: yes
    follow: no

- name: configure vscode
  git:
    accept_hostkey: yes
    repo: "git@github.com:f-koehler/config-vscode.git"
    dest: "~/.config/Code - OSS"
  notify:
    - install vscode extensions


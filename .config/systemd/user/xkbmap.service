[Unit]
Description=set keyboard layout for X
Documentation=man:setxkbmap(1)
After=env-xorg.service

[Service]
Type=oneshot
ExecStart=/usr/bin/setxkbmap -layout de -variant nodeadkeys -option compose:caps

[Install]
WantedBy=default.target
